{
  "configuration": {
    "num_voices": 1,
    "sequence_length": 4,
    "pitch_range": [60, 67],
    "rhythm_values": [1, 2, 4]
  },
  "rules": [
    {
      "rule_id": "force_high_notes",
      "rule_type": "wildcard_constraint",
      "wildcard_type": "for_all_positions",
      "constraint": "voice[v].pitch[i] >= 65",
      "description": "Force all notes to be F4 or higher (MIDI 65+)"
    }
  ]
}
